mutation UpdateAddUserFriend(
    $access: String!,
    $gameId: String!,
    $gameMode: String!
) {
    updateAddUserFriend(
        access: $access,
        gameID: $gameId,
        gameMode: $gameMode
    ) {
        success
    }
}